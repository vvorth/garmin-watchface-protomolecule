project.manifest = manifest.xml

# The compiler defaults to these, but stating them keeps the build predictable
# regardless of how the editor discovers the project.
base.sourcePath = source
base.resourcePath = resources
